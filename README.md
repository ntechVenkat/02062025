1.We can see the magic result of auto-filtering, By placing the file name containing MultipleFilteringNoButton.js and run the project into our App.js react component of our React Project  
2.Here we can filter the data by using the text which existance of specific fields those you want to filter.  
